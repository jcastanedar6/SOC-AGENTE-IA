[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    unit: Unit tests for skills and components
    integration: Integration tests
    bdd: Behavior-driven tests
